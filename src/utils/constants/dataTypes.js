/**
 * Data types used by useDataWithCitiesHandler.
 * @typedef {Object} DataType
 * @property {string} weather - Current and forecast weather data.
 * @property {string} airPollution - Air pollution data.
 * @property {string} longTermWeather - Long-term weather data.
 */

/**
 * Data types used by useDataWithCitiesHandler.
 * @type {DataType}
 */
export const DATA_TYPE = {
  weather: 'weather',
  airPollution: 'airPollution',
  longTermWeather: 'longTermWeather',
};
